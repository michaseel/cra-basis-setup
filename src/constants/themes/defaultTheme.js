import { red500, teal800, black, white } from '../colors/colors';

const defaultTheme = {
  colors: {
    primary: red500,
    secondary: teal800,
    default: black,
    background: white,
  },
};

export default defaultTheme;
